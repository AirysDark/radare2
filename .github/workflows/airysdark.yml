name: build-android-r2

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            meson ninja-build \
            python3 python3-pip \
            gperf cabextract unzip \
            clang file wget

      - name: Install Android NDK
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r26d-linux.zip
          unzip -q android-ndk-r26d-linux.zip

      - name: Create Meson cross file
        run: |
          NDK_PATH="$PWD/android-ndk-r26d"
          TOOLCHAIN="$NDK_PATH/toolchains/llvm/prebuilt/linux-x86_64"
          SYSROOT="$TOOLCHAIN/sysroot"

          cat <<EOF > android-aarch64.txt
          [binaries]
          c = '${TOOLCHAIN}/bin/clang'
          cpp = '${TOOLCHAIN}/bin/clang++'
          ar = '${TOOLCHAIN}/bin/llvm-ar'
          strip = '${TOOLCHAIN}/bin/llvm-strip'

          [properties]
          sys_root = '${SYSROOT}'
          c_args = ['--target=aarch64-linux-android24']
          c_link_args = ['--target=aarch64-linux-android24']

          [host_machine]
          system = 'android'
          cpu_family = 'aarch64'
          cpu = 'aarch64'
          endian = 'little'
          EOF

      - name: Configure
        run: |
          meson setup build \
            --cross-file android-aarch64.txt \
            --buildtype release

      - name: Build
        run: |
          ninja -C build -j2

      - name: Extract r2
        run: |
          mkdir -p dist
          cp build/binr/radare2/r2 dist/r2
          file dist/r2

      - uses: actions/upload-artifact@v4
        with:
          name: r2-android-arm64
          path: dist/r2
